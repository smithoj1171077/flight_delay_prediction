[build-system]
requires = ["setuptools>=65.0"]
build-backend = "setuptools.build_meta"

[project]
name = "airbnb_prediction"
version = "0.1.0"
description = ""
requires-python = ">=3.10"


[tool.setuptools.packages.find]
where = ["src"]
